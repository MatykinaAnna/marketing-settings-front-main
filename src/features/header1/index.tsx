import { loadMarcetingProgram } from 'features/marketingProgram'
import { loadDC } from 'features/settings1'
import {
  setMarketingProgram,
  setCurrent,
  setAuthor,
  setEndDate,
  setStartDate,
  seDivisionProgram,
} from './model/header1-reducer'
export {
  loadMarcetingProgram,
  setMarketingProgram,
  setCurrent,
  setAuthor,
  setEndDate,
  setStartDate,
  loadDC,
  seDivisionProgram,
}
